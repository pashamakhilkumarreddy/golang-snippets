# Wait Groups and Worker pools in Golang

`go run main.go`

`go run --work main.go`

`go build && ./worker-pools`

`go build main.go && ./main`

`go install && worker-pools`

`go install . && worker-pools`
